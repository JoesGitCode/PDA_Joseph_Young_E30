### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby

class CardGame


  def checkforAce(card)
    if card.value = 1
      # needs to be ==
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2)
  # change dif to def
  # missing comma in arguments
  # if statement should be indented
  if card1.value > card2.value
    return card
    # variable should be card1
  else
    return card2
  end
end
end
# extra end

def self.cards_total(cards)
  # using self but it's outside the class
  total
  # total is undefined(should be = 0)
  for card in cards
    total += card.value
    return "You have a total of" + total
    #trying to add int to string(use interpolation)
    # return should be after the for loop
  end
end
```
